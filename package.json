{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "dev": "nodemon --watch src --exec \"tsc && func start\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@azure/identity": "^4.11.1",
    "@azure/openai": "^2.0.0",
    "@azure/storage-blob": "^12.28.0",
    "node-ical": "^0.20.1",
    "openai": "^5.13.1"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "azure-functions-core-tools": "^4.x",
    "nodemon": "^3.1.10",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
